<UserControl x:Class="CombatAnalysis.App.UserControls.ModalWindows.PlayerStats"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CombatAnalysis.App.UserControls.ModalWindows"
             xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
             xmlns:mvx="clr-namespace:MvvmCross.Platforms.Wpf.Binding;assembly=MvvmCross.Platforms.Wpf"
             xmlns:converters="clr-namespace:CombatAnalysis.App.Converters">

    <Grid>
        <Grid.Resources>
            <converters:TheNativeBoolToVisibilityConverter x:Key="BoolToVisibility" />
        </Grid.Resources>

        <Grid Visibility="{Binding Path=IsOpen, ElementName=Root, Converter={StaticResource BoolToVisibility}}"
              Background="{Binding Path=OverlayBackground, ElementName=Root}"
              IsHitTestVisible="{Binding Path=IsOpen, ElementName=Root}">

            <Border Background="{Binding ModalBackground, ElementName=Root}"
                Width="{Binding Path=ModalWidth, ElementName=Root}"
                Height="{Binding Path=ModalHeight, ElementName=Root}"
                Padding="15"
                CornerRadius="25"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

                <Grid Background="#283a4b">
                    <TextBlock Grid.Row="0" Text="{Binding Path=Title, ElementName=Root}" Foreground="White"
                           FontWeight="Bold" FontSize="16" 
                           HorizontalAlignment="Center"
                           Margin="0 20"/>
                    <ContentPresenter Content="{Binding Content, ElementName=Root}" 
                                  Margin="0,30,0,0"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>

</UserControl>
