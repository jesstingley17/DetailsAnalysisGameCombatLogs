apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ .Values.zookeeper.name }}
  labels:
    env: {{ .Values.env }}
    owner: OlegFedosov
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-{{ .Values.zookeeper.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.zookeeper.service.port }}
      targetPort:  {{ .Values.zookeeper.service.targetPort }}