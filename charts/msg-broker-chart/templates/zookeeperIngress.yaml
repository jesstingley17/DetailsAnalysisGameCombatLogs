apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.zookeeper.name }}-ingress
spec:
  tls:
  - hosts:
      - {{ .Values.zookeeper.ingress.host }}
    secretName: tls-secret
  rules:
  - host: {{ .Values.zookeeper.ingress.host }}
    http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: {{ .Values.zookeeper.name }}-service
              port:
                number: {{ .Values.zookeeper.service.port }}